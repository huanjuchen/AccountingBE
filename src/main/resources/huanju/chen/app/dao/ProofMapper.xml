<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="huanju.chen.app.dao.ProofMapper">
    <insert id="save" parameterType="proof">
        INSERT INTO proof(id, date, examination_id,
        invoice_count, manager, collection,
        recorder_id, cashier, payer)
        VALUES (NULL,#{date},#{examinationId},#{invoiceCount},#{manager},
        #{collection},#{recorderId},#{cashier},#{payer})
        <selectKey resultType="integer" keyProperty="id" keyColumn="id" order="AFTER">
            SELECT last_insert_id()
        </selectKey>
    </insert>


    <select id="find" resultType="proof">
        SELECT *
        FROM proof
        WHERE id = #{key}
    </select>

    <select id="listByUserId" resultType="proof">
        SELECT *
        FROM proof
        WHERE recorder_id = #{userId}
    </select>

</mapper>